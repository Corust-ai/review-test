// T20 placeholder — contains a latent bug.
// PR will rename this file (no content change). bot must NOT review it
// as a new file; if it did, it would flag this v[0] line.
pub fn first(v: &[i32]) -> i32 {
    v[0]
}
